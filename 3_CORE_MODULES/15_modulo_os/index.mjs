import os from 'os'

//quantos cpus tem na máquina
console.log(os.cpus());
// memória livre
console.log(os.freemem());
//diretório home
console.log(os.homedir());
//sistema operacional
console.log(os.type());