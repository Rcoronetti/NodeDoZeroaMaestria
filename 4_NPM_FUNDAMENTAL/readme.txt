Para instalar um pacote utilizamos o comando npm install nome;
Dessa maneira uma pasta nome_modules é criada;
Nela todos os arquivos de módulos de terceiro são armazenados;
Sempre que rodamos o comando npm install, a pasta node_modules é recriada com todos os módulos do package.json;