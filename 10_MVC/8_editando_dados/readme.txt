O primeiro passo da edição nós já sabemos: resgatar dado e colocar no formulário em um nova view;
Para isso vamos precisar de uma função no controller, pegando os dados via model (find);
E então criar a rota que corresponde a um id de uma Task, neste caso;
Preenchendo os valores dos inputs com o que foi resgatado do banco.